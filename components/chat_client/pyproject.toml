[project]
name = "chat-client-chat-client"
version = "0.1.0"
description = "Chat client subpackage"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"